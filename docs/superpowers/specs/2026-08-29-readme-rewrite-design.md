# README 重写设计文档

日期：2026-08-29
状态：已获用户批准

## 背景与目标

现有 README 存在以下问题：

1. UI 已重设计（见 `2026-08-29-ui-redesign-design.md`），README 中的截图（GitHub user-attachments 外链）与"界面功能详解"描述的左右分栏布局均已过时，与当前代码（图片池 + 分组导出 + 底部操作栏）不符。
2. 内容大量重复："功能亮点"、"界面功能详解"、"特色功能"三处描述同一批功能。
3. 风格问题：大量 emoji、颜文字、删除线俏皮话（如"等我找到免费域名再说"），不符合专业开源项目门面要求。

目标：重写为单文件中文专业风 README，配真实新 UI 截图与 SVG 示意图，约 150 行。

## 用户已确认的决策

| 决策点 | 结论 |
|--------|------|
| 图片方案 | 真实截图（桌面 + 手机）+ 少量 SVG 示意图 |
| 图片存放 | 仓库 `docs/images/` 目录 |
| 语言风格 | 中文专业风，去 emoji，精炼清晰 |
| 内容组织 | 单文件精简完整版（约 150 行），不拆分子文档 |

## 新 README 结构

| 章节 | 内容要点 |
|------|----------|
| 标题 + 简介 | 一句话定位；徽章：MIT License、GitHub Pages 在线地址、纯前端零依赖；桌面 Hero 截图 |
| 功能特性 | 合并三处重复内容为 6–8 条精炼要点（分组拼接、EXIF 时间排序、拖拽/触屏、浏览器端处理、实时预览、1080px 输出） |
| 界面概览 | 配新 UI 截图；按当前 `index.html` 结构描述：图片池、参数行（每组数量/自动分组/质量）、分组区、底部操作栏（参数/进度/清空/开始拼图）、预览与结果浮层 |
| 快速开始 | 在线使用（https://piggywu981.github.io/MomentWebStitcher/）；本地运行：Python http.server / npx http-server / 直接打开 index.html |
| 使用指南 | 基本流程（上传 → 分组 → 拼图 → 保存）；触屏手势用表格（长按拖拽 400ms、移动放置、组内排序、返回图片池、删除）；输出参数（质量 70–100%、每组数量、1080px 宽度限制） |
| 技术实现 | 配 SVG 流程示意图；技术栈/浏览器兼容/支持格式用表格；说明 exif-js（cdnjs）拍摄时间排序；`MAX_OUTPUT_WIDTH = 1080` 与 `MAX_CANVAS_AREA = 16777216` 的原理（微信二次压缩、规避移动端 Safari 大 canvas 空白） |
| 开发与部署 | 项目结构树（与实际文件一致）；Playwright 冒烟测试运行方式；GitHub Pages 部署步骤（含 deploy.yml 自动部署说明） |
| FAQ | 精选 6–8 条：处理慢、浏览器支持、文件大小、顺序调整、HEIC 跳过、横向拼接 |
| 许可证 | MIT，链接 LICENSE |

## 图片产出清单（`docs/images/`）

| 文件 | 内容 | 获取方式 |
|------|------|----------|
| `screenshot-desktop.png` | 桌面视图 1280px 宽，已上传示例照片并完成分组的完整界面 | Playwright 无头截图，临时脚本执行 |
| `screenshot-mobile.png` | 手机视图 375px 宽，展示响应式布局 | 同上，改 viewport |
| `workflow.svg` | 处理流程示意图：图片池 → 自动分组/拖拽调整 → Canvas 拼接 → 保存；标注 EXIF 排序与 1080px 限制 | 手写 SVG |

截图要求：

- 界面需填充示例照片（程序化生成，如带不同色块与编号的照片），使截图有真实使用感。
- 临时脚本与生成的示例图片放在临时工作目录，不进入仓库；仅最终截图进入 `docs/images/`。

## 内容修正清单

1. 全文去 emoji / 颜文字；删除"在线使用"处的删除线俏皮话。
2. 合并"功能亮点 / 界面功能详解 / 特色功能"三处重复描述。
3. 界面布局描述与当前 `index.html` 实际结构对齐。
4. 保留并核实仍准确的技术细节：质量 70–100%、每组数量可调、HEIC 无法解码自动跳过、`MAX_OUTPUT_WIDTH`、`MAX_CANVAS_AREA`。
5. 技术栈补充 exif-js 用于拍摄时间排序。
6. 删除或修正过时表述，确保"项目结构"树与仓库实际文件一致。
7. 徽章使用 shields.io 静态徽章（MIT、GitHub Pages 在线、纯前端），不引入构建状态徽章（无 CI 测试工作流）。

## 验收标准

1. README 全文无 emoji、无颜文字、无删除线俏皮话。
2. 引用的本地图片（`docs/images/*.png`、`workflow.svg`）均存在于仓库且能正常渲染。
3. 章节结构与本设计文档一致；全文约 150 行（±30%）。
4. 所有功能描述与当前代码行为一致（抽查 index.html / script.js 关键常量与交互）。
5. 仓库中无临时脚本、无多余示例图片残留。

## 非目标

- 不修改任何源代码（index.html / script.js / style.css）。
- 不翻译为英文或制作双语版本。
- 不拆分 README 为多个子文档。
- 不更新 CODE_REVIEW.md 或其他文档。
